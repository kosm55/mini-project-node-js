export enum TableNameEnum {
  USERS = 'users',
  TOKENS = 'tokens',
  CAR_POSTS = 'car_posts',
  CAR_BRANDS = 'car_brands',
  CAR_MODELS = 'car_models',
  CURRENCY = 'currency',
  REGIONS = 'regions',
}
